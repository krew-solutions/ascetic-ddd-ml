(** The session over an HTTP client: scopes with no transaction behind them, and requests
    timed for the observer. See {!Rest_session}. *)

module Rest_session = Rest_session
module Rest_session_pool = Rest_session_pool
module Rest_observer = Rest_observer
