(** Composite sessions: two sessions, or two pools, acting as one. See
    {!Composite_session} for what a composite is and is not. *)

module Composite_session = Composite_session
module Composite_session_pool = Composite_session_pool
