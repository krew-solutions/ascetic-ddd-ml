# Previous implementations, kept for comparison

The outbox and the inbox as they were before the port from the Rust
reference, with their tests. Not built: the root `dune` file marks this
directory as data. To be deleted once the comparison is done.
