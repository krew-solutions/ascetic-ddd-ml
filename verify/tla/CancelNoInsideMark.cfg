\* A handler cancelling its own subscription waits like anybody else.  TLC
\* must find a handler that never returns: it waits for itself.
SPECIFICATION Spec
CONSTANTS
  Callers = {d1}
  Cancellers = {c1}
  Messages = 1
  AdmitUnderLock = TRUE
  WaitForDetach = TRUE
  RegisterUnderLock = TRUE
  InsideMark = FALSE
PROPERTIES
  HandlerReturns
